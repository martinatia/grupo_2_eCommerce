<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/login.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Fira+Sans:ital,wght@0,500;1,600&family=Roboto:wght@700&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet" />
  <title>Iniciar sesión</title>
</head>

<body>
  
  <%- include("../partials/userHeader")%>
  
  <main>
    
    <div class="contenedor-principal">
      <h3>Bienvenido/a</h3>
      <p>Inicia sesión o <a href="/users/registration">crea una cuenta</a></p>
      
      <div class="contenedor-formulario">
        
        <form action="/users/login" method="POST">
          
          <div class="input">
            <i class="fas fa-envelope"></i>
            <input type="text" placeholder="Introduzca su email" name="email" class="<%= (locals.errors && errors.email) ? 'error' : 'success' %>">
          </div>
            <% if(locals.errors && errors.email){ %>
              <div class="error-message">
                <span><%= errors.email.msg %></span> 
              </div>      
            <%}%>
          
          
          <div class="input">
            <i class="fas fa-lock"></i>
            <input type="password" placeholder="Introduzca su contraseña" name="password" class="<%= (locals.errors && errors.password) ? 'error' : 'success' %>">
          </div>
          
          <% if(locals.errors && errors.password){ %>
            <div class="error-message">
              <span><%= errors.password.msg %></span>  
            </div>    
          <%}%>

          <div class="remember">
            <label for="remember">Recordame</label>
            <input type="checkbox" name="remember">
          </div>
          
          <div class="botones">
            <button type="submit">Iniciar sesión</button>
          </div>
          
          
        </form>

      </div>
    </div>
  </main>
  <footer>
    <p>Copyright® 2023 DH Venture Capitals. Todos los derechos reservados</p>
  </footer>
</body>

</html>