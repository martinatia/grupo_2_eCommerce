<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/css/create-product.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css" />
  <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet" />
  <title>Nuevo producto</title>
</head>

<body>

    <%- include("../partials/header")%>
    
    <main>
      
      <h3>Cargá la información del producto:</h3>
      
      <div class="contenedor-principal">
        <form action="/products/new-product" method="POST" class="formulario" enctype="multipart/form-data">
          
          <div class="form-label">
            <label for="name">Nombre del producto:</label>
            <input type="text" id="name" name="name" placeholder="Ej: Zapatillas Reebok" class="form-input" />
          </div>

          <div class="form-label">
            <label for="price">Precio del producto:</label>
            <input type="number" id="price" name="price" placeholder="Ej: 5678" class="form-input" />
          </div>

          <div class="form-options">
            <label for="category">Categoría:</label>
            <select name="category" id="category">
              <option value="">Elegí</option>
              <% for(let i = 0; i < categories.length; i++){%>  
                <option value=<%= categories[i].category_description %>><%= categories[i].category_description %></option>
              <%}%>
            </select>
          </div>
          
          <div class="form-options">
            <label for="brands">Marca:</label>
            <select name="brands" id="brands">
              <option value="">Elegí</option>                                   
            <% for(let i = 0; i < brands.length; i++){%>  
              <option value=<%= brands[i].brand_name %>><%= brands[i].brand_name %></option>
            <%}%>
            </select>
          </div>
          
          <div class="contenedor-secundario">
            <label class="titulo-talles">Talles:</label>
            
            <div class="checkbox-talles">
              <% for(let i = 0; i < sizes.length; i++){%>
              
                <label for="talle<%= sizes[i].size_name%>"></label>
                <%= sizes[i].size_name %>
                <input type="checkbox" id="talle<%= sizes[i].size_name%>" name="talle" value="<%= sizes[i].size_name%>"/>
              
              <% } %>
            </div>
          </div>

          
          <!-- 
        
          Para hacer mas simple la base de datos incluimos los colores en le titulo del producto
          
          <label>Colores:</label>
          <div class="contenedor-secundario">
            <% const colores=["Rojo", "Azul" , "Verde" , "Blanco" , "Negro" , "Rosa" , "Amarillo" , "Violeta" , "Marrón"]%>
              <% for(let i=0; i < colores.length; i++){%>
                <%let color=colores[i]%>
           
                    <label for="color<%=color%>">
                      <%=color%>
                    </label>
                    <input type="checkbox" id="color<%=color%>" name="color" value="<%=color%>" />
                
              <% } %>
           
              
          </div>
          -->

          <div class="form-description">
            <label for="description">Descripción:</label>
            <textarea name="description" id="description" class="form-input"></textarea>
          </div>
          
          <div class="form-images">
            <label for="imagenProduto">Selecciona una imagen:</label>
            <input type="file" id="imagenProduto" name="imagenProduto" accept="image/*">
          </div>
          
          <div>
            <label for="section">Sección</label>
            <div class="form-options">
              <label>
                <input type="radio" name="section" value="in-sale"> Ofertas
              </label>
              <label>
                <input type="radio" name="section" value="features"> Destacados
              </label>
              <label>
                <input type="radio" name="section" value="last-colection"> Última colección
              </label>
            </div>
          </div>

          <div class="botones">
            <button type="reset">Limpiar campos</button>
            <button type="submit">Guardar producto</button>
          </div>

        </form>
      </div>

    </main>
    <%- include("../partials/footer")%>
</body>

</html>